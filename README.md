# Dashboard Backend

This is the backend for the dashboard built with Next.js.  
It includes API routes, controllers, database connection, authentication, and media upload configuration (Cloudinary).  

## Setup

1. Install dependencies:

`bash
npm install